sounddevice>=0.5.0
numpy>=1.26.0
faster-whisper>=1.1.0
silero-vad>=3.1.0
openai>=1.0.0
kokoro>=0.9.4
pydantic>=2.0.0
pyyaml>=6.0
onnxruntime>=1.18.0
tokenizers==0.22.2
python-dotenv>=1.0.0
openwakeword>=0.6.0
pyaudio>=0.2.14
python-statemachine>=3.0.0

# Tools
ddgs>=9.0.0
trafilatura>=2.0.0
httpx>=0.28.0
psutil>=6.0.0

# Anthropic Claude (optional — only if configured)
# anthropic>=0.40.0

# Google Gemini (optional — only if configured)
# google-genai>=1.0.0

# Development
pytest>=9.0
pytest-asyncio>=0.25
pytest-mock>=3.0
